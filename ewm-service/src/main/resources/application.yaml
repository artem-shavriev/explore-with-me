server:
  port: 8080

stats-server:
  #url: http://stats-server:9090
#  локально
  url: http://localhost:9090

spring:
  jpa:
    hibernate:
      ddl-auto: none
    properties:
      hibernate:
        format_sql: true
  sql:
    init:
      mode: always

  datasource:
#    локально
    url: jdbc:postgresql://localhost:7432/ewm-db
    #url: jdbc:postgresql://ewm-db:5432/ewm-db
    driver-class-name: org.postgresql.Driver
    username: user
    password: pass
